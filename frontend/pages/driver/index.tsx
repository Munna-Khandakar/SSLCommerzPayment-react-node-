import GeneralLayout from '@components/Pages/GeneralLayout';
import DriverHome from '@components/Pages/driver/DriverHome';
import React from 'react';

const index = () => {
  return (
    <GeneralLayout>
      <DriverHome />
    </GeneralLayout>
  );
};

export default index;
